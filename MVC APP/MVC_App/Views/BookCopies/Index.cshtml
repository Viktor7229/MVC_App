@model IEnumerable<MVC_App_Domain.Models.BookCopies>

@{
    ViewData["Title"] = "Index";
}

<h1>BookCopies</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CopiesFK_Book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CopiesNumberOfCopies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CopiesFK_Library)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CopiesFK_Book)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CopiesNumberOfCopies)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CopiesFK_Library)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
